import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        borderWidth: 1,
        borderColor: '#ccc',
        borderRadius: 8,
        padding: 8,
        marginBottom: 8,
    },
    title: {
        fontWeight: 'bold',
        fontSize: 20,
        marginBottom: 8,
    },
    subtitle: {
        fontWeight: 'bold',
        marginBottom: 8,
    },
    success: {
        color: 'green',
        marginTop: 8,
    },
});
